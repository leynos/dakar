# Repository-specific formal names and machine interfaces.

schema = 1

[oxford]
stems = []

[words]
accepted = []

[words.corrections]

[patterns]
ignore = [
  # CodeRabbit configuration keys are external schema contracts.
  '(?m)^  labeling_instructions:$',
  '(?m)^  labeling:$',
  '(?m)^    labeling_instructions:$',
  # Inline code spans are masked; the shared dictionary does not mask them yet.
  '`[^`\n]+`',
]

[files]
exclude = []
